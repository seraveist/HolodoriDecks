# 로컬 테스트 방법

## 1. 언어팩 생성

Node.js 24 이상이 설치된 저장소 루트에서 먼저 실행합니다.

```bash
node scripts/build-i18n.mjs
```

정상 완료 시 다음 파일이 생성됩니다.

```text
data/generated/i18n/ko.json
data/generated/i18n/en.json
data/generated/i18n/ja.json
data/generated/i18n/manifest.json
```

이 단계는 단순 다운로드가 아니라 다음 내용을 검증합니다.

- core `master_version`과 KO/EN/JA upstream `version.txt` 일치
- 카드·캐릭터·악곡·리더/액티브/패시브/스페셜 스킬의 필수 LangId 존재
- `Card.json.nameLangId`가 비표준 `la-card_flavor-*`를 사용하는 카드명 정규화

언어팩을 만들지 않고 실행해도 한국어는 core 데이터로 동작하며, 영어·일본어는 manifest에 고정된 upstream commit을 fallback으로 읽을 수 있습니다. 반복 테스트와 오프라인 테스트에는 언어팩을 먼저 생성하는 것을 권장합니다.

## 2. 로컬 서버 실행

HTML module과 `fetch()`를 사용하므로 `index.html`을 직접 더블클릭하지 않습니다.

```bash
python -m http.server 8000
```

Windows에서 `python` 명령이 없다면:

```bash
py -m http.server 8000
```

브라우저에서 `http://localhost:8000/`을 엽니다.

## 3. 다국어 확인

상단 언어 선택기에서 아래 순서로 확인합니다.

1. 기본 한국어 화면이 정상적으로 표시되는지 확인합니다.
2. `English`로 변경하면 화면 UI, 캐릭터명, 카드명, 악곡명, 리더/액티브/패시브/스페셜 설명이 영어로 바뀌는지 확인합니다.
3. `日本語`로 변경하면 같은 항목이 일본어로 바뀌는지 확인합니다.
4. 언어 변경 후 페이지가 다시 로드되어도 선택 언어가 유지되는지 확인합니다.
5. 언어를 바꾸기 전에 등록한 보유 카드, 레벨, 개화, 고정 프리셋이 그대로 유지되는지 확인합니다.
6. 검색과 캐릭터명/카드명 정렬이 현재 언어 문자열을 기준으로 동작하는지 확인합니다.
7. 카드 상세 정보와 추천 결과의 스킬 설명까지 현재 언어로 표시되는지 확인합니다.
8. 결과의 P/T/S 값, 유닛 스코어, 추천 순위가 언어 변경만으로 달라지지 않는지 확인합니다.

특히 신규 여름 카드 7장은 master의 `nameLangId`가 일반 `la-card_name-*`가 아니라 `la-card_flavor-*`를 사용하므로 영어/일본어 카드명이 한국어 fallback으로 남지 않는지 확인합니다.

## 4. 기존 편성 기능 회귀 테스트

1. `내 보유 카드` 탭에 ★4·★5 카드가 표시되고 미보유 이미지는 회색, 보유 이미지는 원본 색상으로 표시되는지 확인합니다.
2. 기본 정렬과 인게임 캐릭터순, 추천력, 희귀도, 캐릭터명 정렬을 확인합니다.
3. 카드를 여러 장 등록하고 레벨·개화를 설정한 뒤 새로고침 후에도 유지되는지 확인합니다.
4. `JSON 내보내기`와 `JSON 가져오기`로 보유 카드 ID·레벨·개화가 복원되는지 확인합니다.
5. 리더 또는 멤버 일부를 프리셋에 고정하고 `고정` 표시가 붙는지 확인합니다.
6. 프리셋 카드 아래에 실제 보유 설정과 같은 `LvN · 개화 N` 값이 표시되는지 확인합니다.
7. 빈 슬롯은 추천 계산에서 자동으로 채워지고, 계산 결과가 고정 프리셋 자체를 덮어쓰지 않는지 확인합니다.
8. 카드 선택창에는 보유 카드만 표시되는지 확인합니다.
9. 카드 선택창과 보유 목록의 `i` 버튼이 카드 선택 대신 상세 정보창을 여는지 확인합니다.
10. `시뮬레이션 목표`, `육성 반영`, `리더/멤버 분리`를 변경하면 기존 추천 결과가 무효화되는지 확인합니다.
11. 악곡·난이도·AUTO/Manual FC 조건을 바꾼 뒤 다시 계산했을 때 결과가 갱신되는지 확인합니다.
12. 추천 TOP 5가 표시되고 각 결과를 접고 펼칠 수 있는지 확인합니다.
13. 상세 결과에서 유닛 스코어, 파워, P/T/S, 점수 보너스 산식을 확인합니다.
14. 스킬 진단표에서 리더 조건, 액티브 레벨·주기·확률·지속시간·예상 발동 횟수·패시브 상태를 확인합니다.
15. 멤버명에 마우스를 올리거나 키보드 포커스를 주면 액티브·패시브·스페셜 설명 툴팁이 보이는지 확인합니다.
16. 발동 타임라인에서 개인 발동 구간과 팀 전체 기대 커버율, 중복 가능 구간이 표시되는지 확인합니다.
17. 동일 카드 중복이나 리더/멤버 분리 조건이 충돌하면 현재 언어로 사유가 표시되는지 확인합니다.

## 5. 자동 검증

GitHub의 `.github/workflows/validate.yml`은 feature branch와 pull request에서 자동으로 다음 검사를 수행합니다.

- 전체 JavaScript syntax 검사
- 주요 ESM module import/export 해석
- 폐기된 `SLOT_LABELS` 참조 검사
- KO/EN/JA 언어팩 생성
- 필수 LangId 전체 검증
- 대표 번역 샘플 검증
- HTML/JavaScript 앱 버전 일치 검증

`main`에서는 `.github/workflows/pages.yml`이 같은 언어팩 검증을 다시 수행하고 GitHub Pages를 배포합니다.

## 6. 현재 계산 범위

추천 점수는 `Unit Score Engine v0.3`과 정적 악곡 근사 계층을 사용합니다. 카드 레벨·개화·리더/액티브/패시브/스페셜 스킬은 반영하지만, 계정별 Holo멤버 보드·메모리 보정과 실제 노트 타임라인은 아직 포함하지 않습니다.
