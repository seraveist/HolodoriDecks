# 로컬 테스트 방법

## 1. 언어팩 생성

Node.js 24 이상이 설치된 저장소 루트에서 먼저 실행합니다.

```bash
node scripts/build-i18n.mjs
```

정상 완료 시 `data/generated/i18n/` 아래에 `ko.json`, `en.json`, `ja.json`, `manifest.json`이 생성됩니다. 생성 과정에서 core `master_version`과 세 언어 upstream 버전, 필수 LangId, 비표준 카드명 LangId를 검증합니다.

## 2. 로컬 서버 실행

```bash
python -m http.server 8000
```

Windows에서 `python` 명령이 없다면:

```bash
py -m http.server 8000
```

브라우저에서 `http://localhost:8000/`을 엽니다.

## 3. 언어 및 테마 확인

1. 한국어 / English / 日本語 전환 시 UI, 카드·캐릭터·악곡명, 스킬 설명이 같이 바뀌는지 확인합니다.
2. 언어를 바꿔도 보유 카드·레벨·개화·프리셋 상태가 유지되는지 확인합니다.
3. 헤더의 달/해 아이콘 버튼을 누를 때 라이트 ↔ 다크 모드가 즉시 전환되는지 확인합니다.
4. 새로고침 후 마지막으로 사용한 라이트/다크 모드가 유지되는지 확인합니다.
5. 기존 `system` 테마 저장값이 남아 있던 브라우저에서는 최초 로드 시 운영체제 테마를 기준으로 라이트/다크 중 하나로 자동 이관되는지 확인합니다.
6. 다크 모드에서 패널, 입력 필드, 결과 카드, 모달, 스킬 표와 타임라인의 글자/배경 대비가 충분한지 확인합니다.
7. 좁은 화면에서 헤더가 `테마 아이콘 + 언어 셀렉트` 한 줄을 유지하고 `테마`, `언어` 라벨 때문에 별도 행이 생기지 않는지 확인합니다.

## 4. 프리셋 UI 확인

1. `시뮬레이션 목표`, `육성 반영`, `리더/멤버 분리`는 상단 계산 조건 영역에 표시되는지 확인합니다.
2. `프리셋 초기화`는 안내 문구와 슬롯 바로 위에 배치되는지 확인합니다.
3. 리더/멤버 카드를 선택하면 카드 우측 상단에 `×` 버튼이 표시되는지 확인합니다.
4. `×`를 누르면 카드 선택 팝업을 열지 않고 해당 슬롯만 즉시 비워지는지 확인합니다.
5. 카드 본체를 누르면 기존처럼 카드 선택 팝업이 열리는지 확인합니다.
6. 프리셋 초기화는 6슬롯 전체만 비우고 보유 카드 목록은 유지하는지 확인합니다.

## 5. 시뮬레이션 목표 확인

시뮬레이션 목표에는 아래 두 항목만 있어야 합니다.

- `최고 유닛 스코어`
- `최고 잠재 스코어`

P/T/S 목표는 없어야 합니다.

1. `전체 평균 + 최고 유닛 스코어` 계산 시 일반 유닛 스코어 기준 TOP 5가 표시되는지 확인합니다.
2. `전체 평균 + 최고 잠재 스코어` 계산 시 잠재 유닛 스코어 기준 TOP 5가 표시되는지 확인합니다.
3. 특정 악곡에서는 유닛 목표가 예상 평균 스코어, 잠재 목표가 근사 최대 스코어를 기준으로 정렬하는지 확인합니다.
4. 잠재 목표 결과의 메인 수치는 잠재/근사 최대값으로 표시되고, 일반 기대값도 비교값으로 함께 표시되는지 확인합니다.
5. 목표를 바꾸면 기존 계산 결과가 무효화되는지 확인합니다.

## 6. 모바일 결과 카드 확인

600px 이하 화면에서 추천 TOP 결과를 확인합니다.

1. 6장 카드를 한 화면에 강제로 축소하지 않고 각 카드가 약 145~168px 폭을 유지하는지 확인합니다.
2. 카드 영역을 좌우 스와이프/스크롤할 수 있는지 확인합니다.
3. 스크롤 시 카드 단위로 자연스럽게 스냅되는지 확인합니다.
4. 카드 일러스트, 캐릭터명, 카드명이 식별 가능한 크기로 유지되는지 확인합니다.

## 7. 기존 편성 기능 회귀 테스트

1. 보유 카드 등록, 레벨·개화 설정, JSON 내보내기/가져오기가 정상 동작하는지 확인합니다.
2. 카드 선택창에는 보유 카드만 표시되는지 확인합니다.
3. 고정 프리셋은 추천 계산 결과로 덮어쓰이지 않는지 확인합니다.
4. 악곡·난이도·AUTO/Manual FC 변경 후 결과가 갱신되는지 확인합니다.
5. 상세 결과의 유닛 스코어, 잠재 스코어, 종합력, P/T/S, 점수 보너스 산식이 표시되는지 확인합니다.
6. 스킬 진단표와 발동 타임라인, 스킬 툴팁이 정상 표시되는지 확인합니다.
7. 리더/멤버 분리 또는 카드 중복 충돌 시 현재 언어로 오류 사유가 표시되는지 확인합니다.

## 8. 자동 검증

`.github/workflows/validate.yml`은 feature branch와 pull request에서 다음을 자동 검사합니다.

- JavaScript syntax 및 ESM import/export
- `최고 유닛 스코어 / 최고 잠재 스코어` 정렬 함수
- P/T/S 목표 제거 여부
- 슬롯 직접 비우기 UI
- 모바일 결과 카드 규칙
- 라이트/다크 테마 토글과 좁은 헤더 배치
- KO/EN/JA 언어팩 및 필수 LangId
- HTML/JavaScript 앱 버전

`main`에서는 `.github/workflows/pages.yml`이 핵심 검증과 언어팩 빌드를 다시 수행한 뒤 GitHub Pages를 배포합니다.

## 9. 현재 계산 범위

추천 점수는 `Unit Score Engine v0.4-potential`과 정적 악곡 근사 계층을 사용합니다. 카드 레벨·개화·리더/액티브/패시브/스페셜 스킬은 반영하지만, 계정별 Holo멤버 보드·메모리 보정과 실제 노트 타임라인은 아직 포함하지 않습니다.
