# 로컬 테스트 방법

## 1. ZIP 산출물 테스트

`Holodori-DeckSim-static.zip`을 **새 빈 폴더**에 압축 해제합니다. 이전 버전 폴더 위에 덮어쓰면 HTML과 JavaScript가 섞일 수 있습니다. 압축을 푼 폴더에서 터미널을 열고 다음 중 하나를 실행합니다.

```bash
python -m http.server 8000
```

또는 Windows에서 `python` 명령이 없다면:

```bash
py -m http.server 8000
```

브라우저에서 `http://localhost:8000/`을 엽니다.

이전에 같은 주소로 실행한 적이 있다면 기존 서버를 종료하고 새 압축 해제 폴더에서 다시 시작하세요. 수정본은 릴리스 버전으로 브라우저 캐시를 구분하며 카드·악곡 JSON은 매번 동기화 manifest와 함께 확인합니다.

## 2. 확인 항목

1. 카드와 악곡 데이터가 오류 없이 로드되는지 확인합니다.
2. `내 보유 카드` 탭이 데스크톱에서 한 행 5장으로 보이고, 미보유 이미지는 회색·보유 이미지는 원본 색상으로 표시되는지 확인합니다. 타입 아이콘은 이미지가 아닌 하단 이름 영역에 있고 `i` 버튼은 이미지 우상단에 있어야 합니다.
3. 목록에 ★3 카드가 없고, 기본 정렬이 ★5→★4 및 각 희귀도 안의 최신 데이터순인지 확인합니다. `인게임 캐릭터순`에서는 캐릭터 마스터 순서를 따르는지도 확인합니다.
4. 카드를 여러 장 등록하고 카드별 레벨·개화를 입력한 뒤 새로고침 후에도 유지되는지 확인합니다. 카드의 `i` 버튼으로 상세 파라미터·스킬·리더 효과가 열려야 하며, 역할 선택 항목과 미보유 카드의 추천력 문구는 표시되지 않아야 합니다.
5. `JSON 내보내기`로 보유 목록을 저장한 뒤 일부 카드를 변경하고 `JSON 가져오기`로 ID·레벨·개화가 복원되는지 확인합니다.
6. 리더 또는 멤버 일부를 직접 선택해 슬롯에 `고정` 표시가 붙는지 확인합니다.
7. 프리셋에 선택된 카드 아래에 보유 카드 설정과 같은 `LvN · N개화` 값이 표시되는지 확인합니다. `추천 편성 계산`을 누른 뒤 고정 프리셋 슬롯은 그대로이고, 빈 슬롯의 추천 카드는 결과 영역에만 표시되어야 합니다.
8. 여섯 슬롯 중 하나를 눌러 카드 선택 창을 열고, 보유 카드만 표시되는지 확인합니다. 보유 목록과 동일하게 하단 첫 줄이 `타입 아이콘 → ★N → 캐릭터명` 순서이고 `i` 버튼은 이미지 우상단에 있어야 하며, `i` 버튼은 선택 대신 상세 정보창을 열어야 합니다.
9. 검색, 희귀도, 타입 필터와 정렬을 각각 바꿔 봅니다. 카드 선택 팝업을 처음 열었을 때는 ★5→★4 순이며, 같은 희귀도에서는 오래된 데이터 순서가 먼저여야 합니다.
10. ★4/★5 카드는 16:9 원본 이미지로 표시되고 ★3 카드는 목록에 없는지 확인합니다.
11. 프리셋 헤더의 시뮬레이션 목표·육성 반영·리더/멤버 분리·초기화가 모바일에서 자연스러운 2열(소형 화면 1열)로 배치되는지 확인합니다.
12. 프리셋 조건을 바꾸면 기존 결과가 무효화되는지 확인합니다.
13. 악곡·난이도·AUTO/Manual FC 기준을 바꾼 뒤 다시 계산해 추천 순위와 스코어가 갱신되는지 확인합니다.
14. 결과 영역에 서로 다른 추천 편성 TOP 5가 표시되는지 확인합니다.
15. 결과 카드의 헤더와 6카드 이미지 영역을 눌러 접고 펼칠 수 있는지 확인하고, 펼친 뒤 근사 최대 스코어·종합력 산식·세 파라미터를 확인합니다. 연녹색 예상 스코어 영역의 숫자가 진한 색으로 선명하게 보여야 합니다.
16. 스킬 정보 표에서 리더 조건에 해당하는 멤버에게만 `리더 조건` 배지가 표시되는지 확인합니다. 액티브 주기, 발동률, 예상 발동 횟수, 동일 주기 중복과 패시브 활성 여부를 확인하고, 멤버명에 마우스를 올려 액티브·패시브·스페셜 스킬 정보를 확인합니다.
17. 스킬 발동 구간의 `팀 전체` 행에서 예상 커버율이 표시되고, 일반 발동 구간은 청록색·둘 이상이 겹칠 수 있는 구간은 붉은색으로 표시되는지 확인합니다.
18. 보유 카드 중 아무 카드나 리더·멤버 프리셋에 고정할 수 있고, 같은 카드 중복이나 리더/멤버 분리 조건이 충돌할 때 계산 사유가 표시되는지 확인합니다.

추천 점수는 `Unit Score Engine v0.3`과 정적 악곡 근사 계층을 사용합니다. 입력한 레벨·개화 단계는 반영하지만 계정별 보드·메모리 보정과 실제 노트 타임라인은 포함하지 않습니다.

## 3. 정적 호환성 검사

원본 저장소에서는 다음 명령으로 모듈 경로, JSON 스키마, 카드 이미지 정책을 한 번에 검사할 수 있습니다.

```bash
node scripts/validate-static.mjs
```
