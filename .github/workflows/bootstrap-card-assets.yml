name: Bootstrap card portraits

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: bootstrap-holodori-card-assets
  cancel-in-progress: true

jobs:
  bootstrap:
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref || github.ref_name }}
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install project
        run: python -m pip install -e .
      - name: Generate current card data
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: holodori-sync --force
      - name: Import initial portrait snapshot
        run: python scripts/bootstrap_card_portraits.py
      - name: Commit imported portraits
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/cards
          if git diff --cached --quiet; then
            echo "No card portraits to add."
            exit 0
          fi
          git commit -m "Bootstrap card portraits"
          git push
