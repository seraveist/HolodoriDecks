[project]
name = "holodori-decksim"
version = "0.1.0"
description = "Hololive Dreams deck simulator and master-data sync tools"
requires-python = ">=3.11"

[project.scripts]
holodori-sync = "holodori_decksim.sync:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
